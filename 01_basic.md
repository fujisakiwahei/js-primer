# JavaScript 基礎メモ（第1章）

対応元: `01_basic.js`（学習用コメントの整理。実行用スクリプトではない箇所あり）

---

## 1. 実行環境と JavaScript の性質

- ブラウザ・Node.js などは**実行環境**と呼ぶ。実行環境で定義されるオブジェクトなどがあり、仕様が異なる。
- JavaScript は大部分が**オブジェクト**であり、オブジェクト同士のやり取りで成り立っている。

---

## 2. 文法の基本

- **大文字・小文字は区別**される（`name` と `NAME` は別）。
- **セミコロン**は自動挿入されるが、暗黙のルールに頼らないよう**文末に書く**。
- **strict mode**（`"use strict"`）は、レガシーでよくない機能・構文をエラーにする。例: `const` なしの変数定義など。
- **ES モジュール**のコードは最初から厳格モード。ファイルを ES モジュールにするには `<script type="module">` を指定する。

---

## 3. 変数: `const` / `let` / `var`

- **`const`**: 再代入できない変数の宣言と初期値。再代入しようとするとエラー。
- **`let`**: 再代入可能。宣言だけして後から代入も可。
- **`var`**: 再代入可能だが、同名の再宣言ができてしまう（`let`/`const` は防ぐ）。挙動も好ましくないため**避ける**。後方互換のために残っているだけ。
- **変数名のルール**: 半角英字、`_`、`$`、数字（先頭不可）。予約語不可。数字先頭不可は数値リテラルとの衝突回避。`$`、`_`、日本語の変数名も一部可。
- **`const` は「定数」とは限らない**: プリミティブで初期化すれば実質定数。オブジェクトは**プロパティの変更**はできるが、**同じ名前で再代入**はできない。

---

## 4. コンソールとエラー

- 多くの実行環境には**評価結果を表示**する機能がある（ブラウザは開発者ツールのコンソール）。
- **構文エラー**（書き方の誤り）と**実行時エラー**（実行してみないと分からない、API の型など）がある。

---

## 5. データ型

- **動的型付け**: 変数に型はない（それは TypeScript の領域）。ただし値には型がある。
- **大別**: **オブジェクト型**と**プリミティブ型**。
- **`typeof`**: 値のデータ型を取得。
- **オブジェクト型**: 名前の通りオブジェクト。`null` は `typeof` で `"object"` になる（歴史的経緯の仕様）。配列も `"object"`。
- **プリミティブ**: 真偽値、数値、`bigint`、文字列、`null`、`undefined`、シンボル（※メモ上の整理。`typeof null` の注意は上記）。

---

## 6. リテラル

- **リテラル**: データ型の値をコードに**直接書く**方法（値そのもの）。
- **数値**: `1_000_000` のように区切り `_` が使える（評価時は無視）。
- **文字列**: `"` と `'` は同義。改行は `\n`。**テンプレートリテラル** `` ` `` は改行そのまま、`${}` で変数展開。常に `` ` `` だけではなく、ただの文字列は `"`/`'` の方が意図が伝わりやすい・JSON は `""` のみ、など**適材適所**。
- **`null`**: 定義はされているが「何もない」。**`undefined`（未定義）とは別**。
- **オブジェクトリテラル `{}`**: 作成と同時に `key: value`。キーは基本は引用符なし、スペース・ハイフン・数字始まりなどは引用符が必要。
- **参照**: **ドット記法**と**ブラケット記法**。変数をキーに使うときはブラケット。基本はドット、必要なときだけブラケット。

---

## 7. 演算子・分割代入・真偽

- **厳密等価 `===` / 厳密不等価 `!==`**: 型と値が一致するか。**`==` は使わない**（予測しづらい）。
- **分割代入**: 配列 `const [a,b,c] = arr`、オブジェクト `const { key, dog } = obj`（左辺の変数名と右辺のキーが対応）。
- **論理演算子**: `&&` `||` `!`。
- **三項演算子**: `条件 ? 真のとき : 偽のとき`。
- **falsy**: `false`, `undefined`, `null`, `0`, `0n`, `NaN`, `""`。それ以外は truthy 扱い。
- **型変換**: 例としてユーザー入力は文字列 → `Number(input)` で数値化。変換不能なら `NaN`。
- **ヘルパー例**: 空文字列判定 `typeof str === "string" && str.length === 0`。

---

## 8. 関数

- **4 要素**: 関数名、仮引数、本体 `{ }`、返り値 `return`。
- **`return` 省略**: 呼び出し結果は `undefined`。
- **引数の個数**: 少ないと余りは `undefined`、多いと余りは無視。
- **デフォルト引数**: `function f(a = 1) {}`。**`??`** で `null`/`undefined` のときだけ別デフォルトも可。
- **第一級関数**: `()` なしでは値として扱われ、変数に代入できる。
- **アロー関数**: 基本は無名。仮引数 1 つは `()` 省略可。本体 1 式なら `return` と `{}` 省略可。
- **同名関数の重複定義**: 上書きされるので避ける。
- **高階関数**: コールバックを引数に取る関数・メソッド。**コールバック**: 他の関数に渡し、条件・タイミングで呼んでもらう関数。
- **メソッド**: オブジェクトのプロパティとしての関数（機能的差はないが呼び方の区別）。**メソッド短縮記法** `obj.method() {}` が推奨。

---

## 9. 式と文・分岐

- **式**: 値を生成し変数に代入できるもの。
- **`switch`**: 関数と組み合わせ、版など条件に応じた値を返すのに便利。`default` は `else` 相当。

---

## 10. ループ・反復

- **`while`**: 条件が `true` の間。無限ループに注意。**安易に `while` より他で書けないか検討**。
- **`do...while`**: 最低 1 回実行してから条件判定。入力バリデーションの例など。
- **`for`**: `for (初期化; 条件; 増分)`。配列の `length` とインデックス `0` 始まりのズレに注意。
- **`join`**: 区切り文字で連結表示。
- **`forEach`**: 配列の高階関数。`,` 区切りで配列を渡さず複数引数にすると、配列としては渡らない（学習メモの注意）。
- **`break`**: ループだけ終了。`return` は関数全体終了。トップレベルでは `return` 使えないので `break` など。
- **`continue`**: 次の反復へ。
- **`some`**: コールバックが一度でも真なら `true` で終了。結果を変数に入れると読みやすい場合も。
- **`filter`**: 条件に合う要素だけの新配列。
- **`for...in`**: 列挙可能な**キー**を列挙（プロトタイプ由来も含む）。値だけ・自身のプロパティだけなら `Object.keys` / `Object.entries` などが向きやすい。
- **配列メソッド**: 種類が多い → [MDN Array](https://developer.mozilla.org/ja/docs/Web/JavaScript/Reference/Global_Objects/Array) 参照。

---

## 11. オブジェクト（詳細）

- プロパティの集合。**`Object`** ビルトインとメソッド群。
- **プロパティ名と変数名が同じ**ときは `{ americanName }` と省略可能。分割代入でも同様の注意。
- **`{}` は `Object` のインスタンス**（始祖は後述の整理）。
- **分割代入**: 存在しないキーは `undefined`（例外ではない）。
- **ミュータブル**: 後からプロパティ追加可能。可読性のため**定義時にまとめる**のが望ましい。
- **`delete`**: プロパティ削除（演算子の一種）。
- **`in`**: プロパティ存在確認（真偽値）。
- **`Object.hasOwn`**: `in` と似るがプロトタイプ絡みで差が出る（詳細は後で、とのメモ）。
- **オプショナルチェーン `?.`**: 左が `null`/`undefined` ならそれ以上評価せず `undefined`。ドットだけだと存在しないと `TypeError`。
- **`toString`**: オブジェクトに使うと `[object Object]`（汎用表示）。
- **`Object` の静的メソッド（列挙）**:
  - `Object.keys` → キーの配列
  - `Object.values` → 値の配列（ES2017）
  - `Object.entries` → `[キー, 値]` の配列（ES2017）
- **マージ**:
  - `Object.assign(ターゲット, ...ソース)` — 既存オブジェクトに足す用途など。
  - **スプレッド** `{ ...a, ...b }` — 新オブジェクト作成。同名は**後勝ち**。
  - `assign` で空 `{}` にマージすると**浅いコピー**。ネストは参照共有に注意。**ディープコピー**は別途。
- **まとめ（メモ）**:
  - `Object` ビルトインがある
  - `{}` での作成・更新
  - 存在確認は `in` か `Object.hasOwn`
  - `?.` はネストの安全なアクセス
  - インスタンスメソッドと静的メソッドの区別
- **Object とプロトタイプ**: ほぼ全オブジェクトが `Object.prototype` を継承（例外あり）。`prototype` は作成時に付与される特殊オブジェクト。配列・関数も共通で使えるメソッドがある。

---

## 12. 配列

- 配列もオブジェクト。
- **二次元**: `arr[i][j]`。
- **疎な配列**: 未定義要素を含む。`length` と実要素が一致しないことがある。
- **`at(index)`**: `arr[i]` の代替。負数は末尾から。
- **`Array.isArray`**: 配列かどうか（`Object` ではなく `Array` の静的メソッド）。
- **分割代入**: 左辺の変数に対応する要素が入る。余りは無視。
- **インデックス**: 0 始まり。`indexOf`、`findIndex`（条件に合う**最初**のインデックス）、`find`（要素そのもの）、`findLast`（最後）。
- **`slice`**: 範囲コピー。終端は**含まない**（半開区間）。負数・省略の挙動のメモあり。
- **`includes`**: 含むか真偽値のみ。
- **追加・削除**: `push`/`pop`（末尾）、`unshift`/`shift`（先頭）。
- **`concat`**: 結合した**新しい配列**（非破壊）。`push` は元配列を変え、返り値は新しい長さ。
- **スプレッド**: 配列リテラル内で任意位置に展開。
- **`splice`**: 指定位置から削除（破壊的）。
- **`length` への代入**: 切り詰め。`[]` で空にするのは `const` だと再代入不可なので `let` が必要、などの注意。
- **破壊的 vs 非破壊的**: `push`/`pop`/`shift`/`splice` 等は破壊的。`concat` は非破壊。名前だけでは判断しづらいので**都度確認**。`toSpliced` / `toReversed` / `toSorted` など非破壊版も。まず非破壊で書けないか検討。
- **反復メソッド**: `forEach`、`map`、`filter`、`reduce`（メモでは一旦スルー）よく使う。
- **`flat`**: 多次元をフラットに（非破壊）。`Infinity` で深さ無制限。
- **`Object.groupBy`**: 条件で配列を分割（メモ）。
- **考え方（メモ）**: ①何をしたいか明確にする ②適切なメソッドを選ぶ（②は学習＋AIで補助可、①が重要）。
- **メソッドチェーン**: 戻り値に続けてメソッドを適用（例: `concat` の連鎖）。

---

## 13. 文字列

- リテラルと同じ記号を含めるときは `\` でエスケープ。
- 結合は `+` またはテンプレートリテラル。
- **`str[i]`**: インデックスアクセス（イニシャル取得など）。
- **UTF-16**: 見た目 1 文字が常に `length` 1 とは限らない（絵文字など）。
- **`split`**: 区切り文字で配列化。
- **`length`**: 文字列長。
- **`slice`**: 部分文字列（配列と同様のイメージ）。
- **`indexOf` + `slice`**: URL のクエリ以降取得など。
- **検索**: インデックスが欲しい → `indexOf` 系。真偽だけ → `startsWith` / `endsWith` / `includes`。
- **正規表現**: `RegExp` またはリテラル。特殊文字はエスケープや参照 URL（Zenn 記事）のメモ。`search` は `indexOf` の正規表現版。`/^にわ/.test(str)` のような例。**意図が伝わりにくいのでコメント推奨**。置換などは都度調べる。
- **URL 組み立て**: `+` 連結より専用関数。`//` 二重などの安全策のメモ。
- **タグ付きテンプレート**: TODO（難しかった、とのメモ）。

---

## 14. ラッパーオブジェクト・プロトタイプチェーン（メモ）

- プリミティブ（真偽・数値・BigInt・文字列・シンボル）に対応する**ラッパー**（例: `String`）がある。
- `new String("value")` は `typeof` が `object`。`toUpperCase` などはインスタンスメソッド。
- **階層というよりプロトタイプチェーン**: 自分が使いたいメソッドを探し、なければ上へ。リテラルはメソッド呼び出し時に**一時的にボックス化**され、ラッパーのメソッドが使える。**わざわざ `new` しない**でよい。
- 詳細は `docs/object-prototype-chain.md`（メモ参照）。

---

## 15. スコープ

- **スコープ**: 変数・関数が他所から参照できる範囲。内側で定義したものは外から参照できない（関数内・仮引数も同様）。
- **スコープチェーン**: 内側から外側へ順に定義を探す。
- **ブロック `{}`**: `const`/`let` はブロックスコープ。`if`/`while` もブロックで制限。ブロック内で外側の変数を**更新**すれば外からも最新値が見える。
- **グローバル**: ルートに書くとどこからも参照可能。**汚染・ビルトインとの衝突**に注意。**参照範囲は小さく**。
- **モジュールで読み込むべきか**: TODO メモ。
- **巻き上げ**: `function` 宣言は宣言前呼び出しも安定。`const`/`let` は宣言前参照はエラーになりうる。
- **IIFE**: `let`/`const` とブロックスコープでグローバル汚染対策としての必要性は減ったが、**別用途ではまだ使う**。
- **クロージャー**: 内側の関数が外側の変数を参照し続け、状態を持てる。**静的スコープ**と**参照され続けるデータの保持**の組み合わせ。`createCounter` の例。仕組みは難しい、とのメモ。

---

## 16. `this`

- メソッドで使うのが基本。全パターン暗記は不要。
- **グローバルの `this`**: ブラウザではグローバルオブジェクト（`window`）。**モジュールのトップレベルでは `undefined`**。
- **メソッド（通常関数）**: 呼び出し時の**ベースオブジェクト**（`.` の左）が `this`。
- **アロー関数のメソッド**: `this` は**外側スコープの `this`** を継承 → オブジェクトを指さず `window` などになりうる、とのメモ。
- **`this` は実行時に決まる**（暗黙に渡される）。strict mode でメソッド以外の通常関数の `this` は `undefined`。
- **メソッドを変数に入れて呼ぶ**とベースオブジェクトがなく `this` は `undefined`（strict）。
- **対策**: メソッドとして使う／`call`・`apply`・`bind`（メモではあまり使わなそうでスルー）。
- **コールバック内の `this`**: `undefined` になりがち → `that` に退避、または**コールバックにアロー関数**（外側メソッドの `this` を使う）。

---

## 17. クラス

- JS Primer 流の言い方: 構造・動作・状態を定義したものをクラスと呼ぶ。**設計図**、インスタンスは **Figma のコンポーネントとインスタンス**に近い。
- **`class` 構文**、**`constructor`** で初期化（インスタンス生成時に実行）。コンストラクタ省略時は空コンストラクタ相当。
- **クラス式**を変数に代入も可。
- **`new`**: インスタンス化。同じクラスから複数作れば別インスタンス。
- **コンストラクタの `this.x`**: ローカル変数 `x` ではインスタンスに載らない。**インスタンスプロパティ**は `this.` で付ける。
- **クラスは `new` 必須**（関数のように呼ぶと `TypeError`）。
- **プロトタイプメソッド**（メモ上の言い方）: クラスからインスタンスを作る仕組みのイメージ。`インスタンス.メソッド()` で呼ぶ。
- **ゲッター / セッター**: `get 名()` / `set 名(仮引数)`。プロパティのフリをしたメソッド。バリデーション・変換に便利。
- **クラスフィールド（ES2022）**: コンストラクタ外で `count = 0` のように宣言。実行順は**フィールド → コンストラクタ**。`count;` だけ宣言も可（読み手・AI に優しい、とのメモ）。
- **フィールドのアロー関数メソッド**: `this` を固定したいコールバック用、とのメモ。
- **プライベートフィールド `#`**: クラス外からアクセス不可（エディタもエラー表示）。
- **静的メソッド `static`**: インスタンスなしで `Class.method()`。**静的メソッド内の `this` はクラス自身**。静的フィールドも可（`Colors.GREEN` の例）。
- **チェーンの概念メモ**: スコープや階層を上にたどって探す。インスタンス → プロトタイプへメソッド探索。クラスはプロトタイプチェーン上の親、との理解メモ。
- **`extends`**: 継承。ビルトイン `Array` も継承可。`super` で親参照。
- **フロントでのクラス（メモ）**: Nuxt では Pinia＋関数で足りることも多いが、API がクラス形式のこともあるので知識は有用。グローバルに近い状態は Pinia、コンポーネント単位は Composition API の方が馴染みやすい、など。

---

## 18. 例外処理

- **`try` / `catch` / `finally`**: `try` で例外 → 以降スキップして `catch`（エラーオブジェクト）。`finally` は必ず最後に実行。
- エンジンが投げるエラーと、**`throw`** で任意に投げるエラー。 **`Error` オブジェクトを投げる**のが推奨（スタックトレース）。
- **ビルトインエラー**: `ReferenceError`、`SyntaxError`、`TypeError` など → [MDN Errors](https://developer.mozilla.org/ja/docs/Web/JavaScript/Reference/Errors)。
- ログは **`console.error`** がよい。**スタックトレース**: どの経路でエラーになったかの記録。

---

## 19. 非同期処理

- **同期**: 1 行が終わるまで次に進まない。長い処理の積み重ねは UI を止めやすい。
- **非同期**: 完了を待たずに次が進む。**本当の並列とは限らず**、メインスレッドの細切れ処理。Web Worker などは別。
- 非同期は **`async` だけではない**（`setTimeout` なども、とのメモ）。
- **`Promise`**: `new Promise((resolve, reject) => {})`。`then(onFulfilled, onRejected)`。状態は **Pending / Fulfilled / Rejected**。一度決まったら変わらない（Settled）。
- **`fetch`**: 非同期。完了前に戻り値を使うと `undefined` になる例（`getGitUsername` のメモ）。
- **`async` / `await`**: 非同期関数に `async`、`待つ` 箇所に `await`。`then` チェーンより読みやすい、とのメモ。
- 練習: GitHub API、Open-Meteo、**JSONPlaceholder**（`userId=1` の投稿最大 3 件、`id` / `title` / `body` 出力）。
- JS Primer の一部（Promise/Async の深い項）はスキップし動画で補う、とのメモ。**`map` は一旦スキップ**。**`Set`** は重複なし集合、添字はなく `for...of` / `forEach`。**イテレーター・ジェネレーター**は Nuxt/React ではほぼ使わないのでスキップ、とのメモ。

---

## 20. JSON / `Date` / `Math`

- **`JSON.parse`**: `localStorage` は文字列のみなのでオブジェクトの保存・取得とセットで使う。パース失敗は例外 → **`try/catch`**。
- **`Date`**: `new Date()` で現在。数値だけは **`Date.now()`**（静的）。**`date.now()` は不可**（`Date` にのみ存在し、`Date.prototype` にもない）。
- **`getTime()`**: そのインスタンスの時刻値。`toISOString()` で ISO 8601 文字列。
- **`Math`**: 乱数・大小比較・丸めなど数学系。

---

## 21. モジュール

- **Vue/Nuxt**: 多くの `.js` / `.ts` / `.vue` が**モジュール**として扱われ、**ファイル単位でスコープが閉じる**のでトップレベル名の衝突しにくい。
- **Vanilla**: **`type="module"` を付けない**とスクリプトとしてグローバル共有になり**衝突しやすい**（※Vanilla でも `type="module"` ならファイルごとモジュール）。
- **`export` / `import`**: 複数モジュール間で同名があっても、各モジュール内に閉じていれば問題ない、とのメモ。
- **`export default`**: メインの無名エクスポート。呼び出し側で名前を付ける。**名前付き export の方が補完しやすい**ので基本はそちら。Nuxt で「このディレクトリはデフォルトエクスポート」と決まっている箇所（`nuxt.config`、`server/api/*` など）だけ `default`、とのメモ。

---

## 22. 進捗メモ

- `260404` — 第1章終了。
- 本ドキュメントは上記 `01_basic.js` の整理版。

---

## 補足（元ファイルの TODO・参照）

- 正規表現の深掘りは別途学習。
- タグ付きテンプレート: 難しかった（TODO）。
- Object の階層 → プロトタイプチェーン理解に更新（`docs/object-prototype-chain.md`）。
- 非同期理解用 YouTube リンク（メモ内 URL）。
- Zenn: プレーンなブロックでスコープを制限する話、正規表現の特殊文字まとめ（メモ内 URL）。
